apiVersion: batch/v1
kind: Job
metadata:
  name: notification-service-init
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: notification-service-init
          image: campuscore-notification-service
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -lc
            - ./node_modules/.bin/prisma db push --schema prisma/schema.prisma --skip-generate && node dist/prisma/migrate-legacy-notifications.js
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: campuscore-secrets
                  key: POSTGRES_PASSWORD
            - name: DATABASE_URL
              value: postgresql://campuscore:$(POSTGRES_PASSWORD)@postgres:5432/campuscore?schema=notifications
