# Replace every value before deploying to any shared or public environment.
apiVersion: v1
kind: Secret
metadata:
  name: campuscore-secrets
type: Opaque
stringData:
  POSTGRES_PASSWORD: replace-me-before-apply
  JWT_SECRET: replace-me-before-apply
  JWT_REFRESH_SECRET: replace-me-before-apply
  HEALTH_READINESS_KEY: replace-me-before-apply
  INTERNAL_SERVICE_TOKEN: replace-me-before-apply
  RABBITMQ_PASSWORD: replace-me-before-apply
  MINIO_USER: replace-me-before-apply
  MINIO_PASSWORD: replace-me-before-apply
  SMTP_USER: replace-me-before-apply
  SMTP_PASSWORD: replace-me-before-apply
