apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: campuscore-tls
spec:
  secretName: replace-with-real-prod-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: replace-with-real-prod-cluster-issuer
  dnsNames:
    - replace-with-real-prod-host.example.com

