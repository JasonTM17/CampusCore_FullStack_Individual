apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: campuscore-tls
spec:
  secretName: campuscore-prod-tls
  issuerRef:
    kind: ClusterIssuer
    name: replace-with-prod-cluster-issuer
  dnsNames:
    - campuscore.example.com
